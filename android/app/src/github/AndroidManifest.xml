<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Only the GitHub distribution may request package installation. The
         app still asks the user for the per-source grant before installing. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
</manifest>
